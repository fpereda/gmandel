CLEANFILES = *~
MAINTAINERCLEANFILES = aclocal.m4 configure Makefile.in config/config.guess \
                       config/ltmain.sh config/config.sub
AUTOMAKE_OPTIONS = dist-bzip2 no-dist-gzip std-options
EXTRA_DIST = autogen.bash LICENCE

SUBDIRS = src .
